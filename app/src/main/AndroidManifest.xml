<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.xwl.kotlinbasepro">

    <dist:module dist:instant="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- startActivity -->
        <activity android:name=".startActivity.SecondActivity"/>
        <activity android:name=".startActivity.ThirdActivity"/>

        <!-- onclick事件 -->
        <activity android:name=".onclick.FirstOnclickActivity"/>
        <activity android:name=".onclick.SecondOnclickActivity"/>
        <activity android:name=".onclick.ThirdOnclickActivity"/>

    </application>

</manifest>